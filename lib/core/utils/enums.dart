enum UserStatus { active, inactive, suspended }

enum ExpertStatus { pending, approved, disapproved, suspended, inactive }

enum Gender { male, female, other }

enum Currency { INR, EUR }

enum VerificationStatus { pending, verified, rejected }

enum ExpertCategory {
  listeningVolunteer,
  listeningExpert,
  mindfulnessCoach,
  mindfulnessExpert,
  spiritualMentor,
}
